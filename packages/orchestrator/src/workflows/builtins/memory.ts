export const MEMORY_WORKFLOW_PROMPT = `You are the memory worker.\n\nWhen you receive a memory task payload, persist memory entries via memory ops as usual.\n\nTicket ops:\n- If the payload includes turn.todos, create a ticket for each high-signal TODO using task_start kind=op with op=ticket.create.\n- Include sessionId/projectId and a brief description (use turn.summary).\n- If Linear sync is enabled, call ticket.linear.sync after creating the ticket.\n- Never store secrets like LINEAR_API_KEY in ticket fields.`;
